SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PORT=

# Auth
JWT_SECRET=doi-thanh-chuoi-ngau-nhien-dai
JWT_EXPIRES_IN=7d
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5
# Chỉ cho phép email domain này đăng nhập (vd: ozovn.com). Để trống = cho tất cả.
ALLOWED_EMAIL_DOMAIN=ozovn.com
# Mã bí mật chung để PIC nâng quyền lên Quản lý (sửa tất cả). ĐỔI thành chuỗi khó đoán.
MANAGER_CODE=doi-ma-nay
# (Ai là PIC được quản lý trong bảng public.pic_members trên Supabase — không cấu hình ở đây.)

# SMTP gửi mã OTP qua email — để trống sẽ chạy chế độ DEV (in mã ra console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your@ozovn.com
SMTP_PASS=app-password-16-ky-tu
# Tên/địa chỉ người gửi hiển thị (để trống = dùng SMTP_USER)
MAIL_FROM=Feelex QLDA <your@ozovn.com>
